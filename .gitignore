# Build products
build/
# Installs are kept outside the checkout and shown here through a symlink, so
# that a `git clean -xdf` -- which one reaches for exactly when a build is
# broken -- removes the link and not the archive.
builds
*.o
*.a
*.so
*.egg-info/

# Generated by gensetup.py.sh
setup.py

# Local, machine-specific build configuration
Makefile.local
__pycache__/
